server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/orderdb?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  redis:
    host: localhost
    port: 6379
  cache:
    type: redis

  zipkin:
    base-url: http://localhost:9411
    sender:
      type: web
    sleuth:
      sampler:
        probability: 1.0
logging:
  level:
    root: INFO
    com.example.orderservice: DEBUG
    org.elasticsearch.client: DEBUG
    org.elasticsearch.transport: TRACE
    org.springframework.data.elasticsearch.core.ElasticsearchRestTemplate: DEBUG