server:
  port: 8083

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  zipkin:
    base-url: http://localhost:9411
    sender:
      type: web
  sleuth:
    sampler:
      probability: 1.0
logging:
  level:
    root: INFO
    com.example.adminservice: DEBUG
    org.elasticsearch.client: DEBUG
    org.elasticsearch.transport: TRACE
    org.springframework.data.elasticsearch.core.ElasticsearchRestTemplate: DEBUG
